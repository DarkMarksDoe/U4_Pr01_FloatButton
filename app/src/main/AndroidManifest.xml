<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrk2.u4_pr01_floatbutton">

    <application
        android:allowBackup="true"
        android:icon="@drawable/servicio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityTapSensor"></activity>
        <activity android:name=".ActivityCamera" />
        <activity android:name=".ActivityVideoView" />
        <activity android:name=".video_player" />
        <activity android:name=".media_player" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>